{
  "name": "google-photos-instant-swap",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that removes the slide between photos on the Google Photos website, so you can compare two photos with the arrow keys.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit -p jsconfig.json",
    "test": "node --test",
    "check": "npm run format:check && npm run typecheck && npm test",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^26.4.1",
    "lefthook": "^1.10.10",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2"
  }
}
